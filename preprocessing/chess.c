#include <stdio.h>
#include <stdlib.h>

int main(){
    int m, n;
    char ** map;
    int i;
    scanf("%d %d", &m, &n);
    map = (char **)malloc(sizeof(char*)*m);
    for(i=0; i<m; i++){
        map[i] = char(*)
    }
}
